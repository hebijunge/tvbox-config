__pycache__/
*.pyc
node_modules/

live_probe_results.jsonl
live_channels.json
adult_lives_checks*.json
live_checks.json

# SQLite 运行库不入库：二进制会让仓库逐日膨胀。
# 检测历史的可追溯性改由 exports/health_index.json（纯文本，diff 友好）承担——
# 每天 diff 能直接看出哪些源从 healthy 掉成 dead。
state/*.db
state/*.db-journal
logs/
packs/
state/adult_leak_report.json


# iptv-org 原始抓取缓存（channels 7.8MB / streams ~17MB，可由 fetch 重建）；
# fetch_meta.json（溯源 commit_sha）很小，照常入库。
state/iptvorg/channels.json
state/iptvorg/streams.json
state/iptvorg/feeds.json
state/iptvorg/blocklist.json
state/canary/*.bak-*
state/vocab/*.bak-*

# snapshot 止血（2026-09-25 P1 收口，总调度拍板：不做 git 历史重写）：
# 上游原始抓取快照不再每日入 git（累计 172MB/2591 文件）。历史日已归档到
# Release snapshot-archive（snapshot-YYYYMMDD.zip），主干树只保留最新一日
# （由 scripts/snapshot_rotate.py 每日轮换：git rm --cached 旧日 + git add -f 新日）。
snapshot/

# P1-C 承接（2026-09-25）：sync/ 是飞书线临时入库通道，daily-fetch 吸收后与
# tvbox.json 内容重复（实测 JSON 等价、1,070,614 B 双份）。已跟踪的
# sync/feishu_config_latest.json 保留（通道不断，跟踪文件不受 ignore 影响），
# 但不再跟踪新增副本；飞书线后续应改为临时不入库传递或显式 git add -f。
sync/

# .dualcheck/ 对账副本（飞书回读缓存，仅对账运行时需要）不再入库；
# 历史对账副本已由 exports/dual_write_report_*.json 留痕，工作区副本保留。
.dualcheck/
